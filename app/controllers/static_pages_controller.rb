class StaticPagesController < ApplicationController

  # Controller for home page
  # Empty because it's static content. Not retrieved from database
  def home
  end

  # Controller for help page
  # Empty because it's static content. Not retrieved from database
  def help
  end

  # Controller for about page
  # Empty because it's static content. Not retrieved from database
  def about
  end

  # Controller for contact page
  # Empty because it's static content. Not retrieved from database
  def contact
  end
  
  # Controller for terms page
  # Empty because it's static content. Not retrieved from database
  def terms
  end
  
  # Controller for privacy page
  # Empty because it's static content. Not retrieved from database
  def privacy
  end
end
