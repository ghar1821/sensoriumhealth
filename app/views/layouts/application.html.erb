<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title><%= full_title(yield(:title)) %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Rails Bootstrap" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application", "jquery-1.10.2.min", "rails", "highcharts" %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>

    <!-- For Google Analytics -->
    <script>
    // Script for Basic Tracking Functions 
  	(function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  	(i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  	m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  	})(window,document,'script','//www.google-analytics.com/analytics.js','ga');
	
	// Defines tracking code (UX-XXXXX-X) to identify the website. 
	// Tracking code is available from your Google Analytics account. 
	// Also specifies the url of the website.
	// If either of these changes, regenerate a new javascript snippet to ensure tracking is available.
  	ga('create', 'UA-45183428-1', 'sensoriumhealth.herokuapp.com');
  	ga('send', 'pageview');
	</script>

  </head>
  <body>
    <header class="navbar navbar-fixed-top">
      <nav class="navbar-inner" style="background:#53d2f9;">
        <div class="container">
          <table>
          	<tr> 
          	  <td><%= image_tag("brighthearts-logo-transparent.png", :size => "40x40") %></td>
          	  <td><%= render 'layouts/navigation' %></td>
          	<tr>
          </table>
        </div>
      </nav>
    </header>
    <main role="main">
      <div class="container">
        <div class="content">
           <div class="row">
            <div class="span12">
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
          </div>
          <footer class="navbar navbar-fixed-bottom">
          	<nav class="navbar-inner" style="background:#53d2f9;">
          		<div class="container">
					<%= render 'layouts/footer' %>
				</div>
          </footer>
        </div>
      </div>
    </main>
  </body>
</html>
