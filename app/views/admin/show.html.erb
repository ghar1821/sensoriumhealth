<% provide(:title, "admin") %>
<!DOCTYPE html>
<html>
<head>
	
</head>
<body>
	<div>
<table class="table table-hover" border="1" >
<tbody>
  <tr>
    <th>Username</th>
    <th>Email</th>
    <th>Gender</th>
    <th>City</th>
    <th>Last sign in</th>
  </tr>
    <% @users.each do |u| %>
      <tr>
        <td><%= gravatar_for u %>&nbsp;&nbsp;&nbsp;<%= u.username %></td>
        <td><%= u.email %></td>
        <td><%= u.gender %></td>
        <td><%= u.city %></td>
        <td><%= u.last_sign_in_at %></td>
      </tr>
    <% end %>
    </tbody>
</table>
</div>
</body>
<body>

</body>
</html>

